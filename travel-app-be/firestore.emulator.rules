rules_version = '2';

// Emulator-only rules. Production continues to use firestore.rules, which
// denies direct client access; the API uses Firebase Admin for data access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
