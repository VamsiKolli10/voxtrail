# Archived Firebase Functions codebase

The production deployment uses the `backend` codebase rooted at
`travel-app-be/`. This directory is retained only for historical compatibility
and is not included in the active Firebase deployment configuration.

Do not add production functions here. New API work belongs in `travel-app-be/`
and must be exported through `travel-app-be/index.js`.
